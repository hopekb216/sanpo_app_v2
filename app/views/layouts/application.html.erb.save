<!DOCTYPE html>
<html>
  <head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>SanpoApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body data-turbo="false">

    <header class="header">
      <p style="color: blue;">AFTER HEADER</p>
      <p style="color: blue;">AFTER HEADER</p>
      <p style="color:red;">TOP OF APPLICATION</p>

      <p style="color:green;">FULLPATH: <%= request.fullpath %></p>
      <p style="color: purple;">THIS IS REAL LAYOUT</p>

      <div class="header-left">
        <%= link_to "SanpoApp", root_path, class: "header-logo" %>
      </div>

      <div class="header-right">
        <p style="color:orange;">CHECK: <%= request.fullpath %></p>
        <% if user_signed_in? %>

          <%= button_to t("links.logout"), destroy_user_session_path,
                method: :delete,
                form: { data: { turbo: false } },
                class: "header-link" %>

        <% else %>

          <% unless request.fullpath.include?("sign_in") %>
            <%= link_to t("links.login"), new_user_session_path, class: "header-link", data: { turbo: false } %>
          <% end %>

          <% unless request.fullpath.include?("sign_up") %>
             <%= link_to t("links.signup"), new_user_registration_path, class: "header-link", data: { turbo: false } %>
          <% end %>

        <% end %>
      </div>
    </header>

    <% flash.each do |key, value| %>
      <div class="flash <%= key %>"><%= value %></div>
    <% end %>

    <main class="main-content">
      <%= yield %>
    </main>

  </body>
</html>
